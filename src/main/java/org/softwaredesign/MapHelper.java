package org.softwaredesign;

import java.util.List;

public class MapHelper {
    public static void displayMap(List<List<Double>> coordinates){  //coordinates in a format of lat: coordinates.get(i).get(0), lon: coordinates.get(i).get(1)
        //TODO: create map instance:
            //TODO: figure out how to display this or pass the location from the controller
        //TODO: display coordinates on the map
    }
}
